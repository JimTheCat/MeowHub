CREATE OR REPLACE FUNCTION mh_meowhub.get_user_id RETURN VARCHAR2
AS
  l_user_id VARCHAR2(128);
BEGIN
   RETURN nvl(SYS_CONTEXT('USERENV', 'CLIENT_IDENTIFIER'), SYS_CONTEXT('USERENV', 'SESSION_USER'));
END;
/